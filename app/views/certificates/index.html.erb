<div class="container mx-auto px-4 py-8">
  <div class="flex justify-between items-center mb-6">
    <h1 class="text-2xl font-bold text-gray-800">Mis Certificados</h1>
    <%= link_to enrolled_courses_path, class: "text-indigo-600 hover:text-indigo-800" do %>
      <i class="fas fa-arrow-left mr-1"></i> Volver a mis cursos
    <% end %>
  </div>

  <% if @certificates.any? %>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      <% @certificates.each do |certificate| %>
        <div class="bg-white rounded-lg shadow-md overflow-hidden">
          <div class="p-6">
            <% if certificate.course.cover_image.attached? %>
              <%= image_tag certificate.course.cover_image, class: "w-full h-32 object-cover rounded mb-4" %>
            <% else %>
              <div class="w-full h-32 bg-gray-200 rounded mb-4 flex items-center justify-center">
                <i class="fas fa-certificate text-gray-400 text-4xl"></i>
              </div>
            <% end %>
            
            <h3 class="text-lg font-semibold mb-2"><%= certificate.course.title %></h3>
            <p class="text-sm text-gray-600 mb-4">Completado el <%= l certificate.issued_at.to_date, format: :long %></p>
            
            <div class="flex justify-between items-center">
              <%= link_to certificate_path(certificate), class: "text-indigo-600 hover:text-indigo-800" do %>
                <i class="fas fa-eye mr-1"></i> Ver
              <% end %>
              
              <%= link_to download_certificate_path(certificate, format: :pdf), class: "text-green-600 hover:text-green-800" do %>
                <i class="fas fa-download mr-1"></i> Descargar
              <% end %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  <% else %>
    <div class="bg-white rounded-lg shadow-md p-8 text-center">
      <div class="text-gray-400 mb-8">
        <i class="fas fa-certificate text-6xl"></i>
      </div>
      
      <h3 class="text-xl font-medium text-gray-700 mb-4">¡No tienes certificados aún!</h3>
      
      <div class="space-y-4">
        <p class="text-gray-600">Con esfuerzo y dedicación podrás obtener tus primeros certificados. Sigue estos pasos:</p>
        
        <div class="space-y-2">
          <div class="flex items-start space-x-2">
            <span class="text-indigo-600 text-xl">1.</span>
            <div>
              <h4 class="font-medium text-gray-700">Compra un curso</h4>
              <p class="text-sm text-gray-600">Explora nuestro catálogo y elige el curso que más te interese.</p>
            </div>
          </div>
          
          <div class="flex items-start space-x-2">
            <span class="text-indigo-600 text-xl">2.</span>
            <div>
              <h4 class="font-medium text-gray-700">Comienza a aprender</h4>
              <p class="text-sm text-gray-600">Avanza a tu ritmo y completa todas las lecciones.</p>
            </div>
          </div>
          
          <div class="flex items-start space-x-2">
            <span class="text-indigo-600 text-xl">3.</span>
            <div>
              <h4 class="font-medium text-gray-700">Obtén tu certificado</h4>
              <p class="text-sm text-gray-600">Al completar el curso, recibirás tu certificado automáticamente.</p>
            </div>
          </div>
        </div>
      </div>
      
      <div class="mt-8">
        <%= link_to courses_path, class: "inline-flex items-center px-6 py-3 bg-indigo-600 text-white rounded-lg hover:bg-indigo-700" do %>
          <i class="fas fa-graduation-cap mr-2"></i> Empezar a aprender
        <% end %>
      </div>
    </div>
  <% end %>
</div>
